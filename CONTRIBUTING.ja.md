# 貢献方法

[English](CONTRIBUTING.md)

このリポジトリのIssue・PRを使ってください。日本語・英語のどちらでも構いません。不具合は正確なMinecraft/Forge/MODバージョン、JARハッシュ、再現手順、期待値・実際の動作、秘匿情報を除いたログを添えてください。認証情報・world全体・非公開サーバー設定は投稿しないでください。

変更範囲を絞り、必要な検証を行います。アイテム移動・購入・永続化・通信は意味のある回帰検証、GUIは実画面を添えてください。日英文書を同時に更新します。レビュー前に`python3 scripts/check-project.py`、`bash test-core.sh`、Forgeビルドを実行します。

サーバーを正とする設計を維持します。第三者MODのバイナリ追加、実機記録なしの依存更新、未知の保存データの暗黙移行、PR workflowからのRelease公開は行いません。貢献はMITライセンスで提出し、必要な第三者通知を保持してください。
